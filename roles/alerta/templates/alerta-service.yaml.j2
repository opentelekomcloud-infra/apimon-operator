apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  name: "{{ name }}"
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: "{{ name }}"
status:
  loadBalancer: {}
