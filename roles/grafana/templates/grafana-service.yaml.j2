apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  name: "{{ name }}"
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    run: "{{ name }}"
status:
  loadBalancer: {}
